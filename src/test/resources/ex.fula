def addT(x: Int, y: Int) : Int = {
  if (x == 0) { y } else {addT(x - 1, y + 1)}
};

def fib(index: Int, prev: Int, current: Int) : Int = {
  if (index <= 0) {
    current
  } else {
    fib(index - 1, prev + current, prev)
  }
};

println(fib(20,1,0))